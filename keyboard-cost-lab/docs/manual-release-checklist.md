# 배포 전 수동 점검표

부제: 브라우저 자동화의 한계를 보완하기 위한 서비스별 배포 점검표

## 1. 목적

이 문서는 브라우저 자동화로 끝까지 검증하기 어려운 항목을 사람이 직접 확인할 수 있도록 만든 배포용 점검표다. 홈 화면, 서비스 진입, 과업 준비, 새 탭 수행, 결과 저장, 문구 이해 가능성까지 한 번에 점검한다.

## 2. 공통 준비

- [ ] `node scripts/run-benchmark.mjs`를 다시 실행해 사전 계산 결과 파일을 최신 상태로 만들었다.
- [ ] 정적 서버에서 `index.html`, `comments.html`, `product.html`, `search.html`, `settings.html`, `filters.html`, `checkout.html`이 모두 열리는지 확인했다.
- [ ] 새 탭 열기 기능이 브라우저의 팝업 차단에 막히지 않는지 확인했다.
- [ ] `docs` 폴더에 이번 단계 보고서와 이전 단계 보고서가 함께 남아 있는지 확인했다.
- [ ] 용어 가이드 문서가 최신 상태인지 확인했다.

## 3. 공통 화면 흐름 점검

### 3.1 홈 화면

- [ ] 첫 화면에서 바로 과업 시작 버튼이 보이지 않는다.
- [ ] 서비스 유형을 먼저 고른 뒤 해당 서비스 화면으로 들어갈 수 있다.
- [ ] 현재 공개된 서비스 유형이 명확하게 보인다.
- [ ] 홈 화면 문구에 불필요한 영어가 남아 있지 않다.
- [ ] 서비스 카드 버튼은 `예약 캘린더 화면으로 이동`, `검색 결과 목록 화면으로 이동`, `설정 화면으로 이동`, `검색 세부 조건 화면으로 이동`처럼 서비스 이름만 읽어도 서로 구분된다.

### 3.2 서비스 화면

- [ ] 서비스 요약 문단만 읽어도 무엇을 비교하는 실험인지 이해할 수 있다.
- [ ] `과업 준비로 이동`, `서비스 선택으로 돌아가기` 문구가 동작을 분명하게 설명한다.
- [ ] 용어 설명 보기에서 남겨 둔 용어의 뜻을 확인할 수 있다.

### 3.3 과업 준비 화면

- [ ] 과업 시작 전에 목표, 단계, 현재 상태, 목표 대상이 모두 메인 창에 표시된다.
- [ ] 수행 탭을 열기 전에도 과업 정보를 충분히 읽을 수 있다.
- [ ] 새 탭을 열어도 원래 메인 창이 그대로 남아 있어 과업 내용을 다시 확인할 수 있다.
- [ ] 각 서비스에서 비교안 순서는 항상 `A → B`로 먼저 표시된다.
- [ ] 상태 메시지가 `탭 열림`, `실제 계측 시작`, `탭 닫힘`, `결과 전달 완료`를 구분해 보여 준다.
- [ ] 수행 탭의 서비스 콘텐츠 안에는 비교안 설명이나 실험 운영 문구가 남아 있지 않다.

### 3.4 수행 탭 계측 규칙

- [ ] 수행 탭을 열자마자 시간 기록이 시작되지 않는다.
- [ ] 첫 키 입력이나 첫 클릭이 들어가는 순간부터 기록이 시작된다.
- [ ] 수행 탭을 다른 탭으로 가려 두었을 때 숨김 시간이 결과에서 빠진다.
- [ ] 수행 탭 하단의 첫 이동 버튼과 `이 탭 닫기` 버튼 조작은 실제 지표에 포함되지 않는다.
- [ ] 과업을 성공하면 완료 안내 팝업 대화상자가 열리고, `확인` 버튼을 누르면 수행 탭이 자동으로 닫힌다.

### 3.5 언어와 이해 가능성

- [ ] `slot`, `skip link`, `modal`, `focus` 같은 낯선 영어가 화면에 그대로 남아 있지 않다.
- [ ] 버튼과 링크 문구만 읽어도 눌렀을 때 일어나는 결과를 예상할 수 있다.
- [ ] 같은 뜻의 용어가 페이지마다 달라지지 않는다.
- [ ] 설명이 필요한 용어는 용어 설명 보기나 인접 문구에서 뜻을 확인할 수 있다.

## 4. 예약 캘린더 점검표

### 4.1 공통

- [ ] 홈 화면의 `예약 캘린더` 카드에서 서비스 화면으로 들어갈 수 있다.
- [ ] 과업 준비 화면에서 현재 예약 내용과 목표 예약 시간이 보인다.
- [ ] 예약 캘린더는 비교안 A가 먼저 나오고 그다음 비교안 B가 나온다.
- [ ] 수행 탭 하단에는 `예약 가능 시간으로 이동`, `이 탭 닫기` 두 버튼만 보인다.

### 4.2 비교안 A

- [ ] 상단 보조 링크와 조건 선택을 지나야 예약 가능 시간에 도달한다.
- [ ] 예약 시간마다 `선택`, `시간 안내 보기`가 따로 있어 순차 이동이 길다.
- [ ] 대화상자를 닫으면 방금 보던 예약 시간 대신 `예약 가능 시간` 제목 근처로 돌아온다.
- [ ] 과업 1, 2, 3이 모두 끝나면 결과 요약 화면으로 넘어간다.

### 4.3 비교안 B

- [ ] 맨 앞의 `예약 가능 시간으로 바로 이동` 링크로 예약 가능 시간 영역에 바로 갈 수 있다.
- [ ] 예약 시간표는 한 번만 들어간 뒤 방향키로 이동할 수 있다.
- [ ] 대화상자를 닫으면 방금 열었던 예약 시간이나 현재 예약 내용으로 초점이 돌아온다.
- [ ] 조건 적용 후 결과 제목으로 초점이 이동한다.

## 5. 댓글 목록 점검표

### 5.1 공통

- [ ] 홈 화면의 `댓글 목록` 카드에서 댓글 서비스 화면으로 들어갈 수 있다.
- [ ] 과업 준비 화면에서 현재 댓글 상태와 목표 댓글이 보인다.
- [ ] 댓글 목록은 비교안 A가 먼저 나오고 그다음 비교안 B가 나온다.
- [ ] 수행 탭 하단에는 `댓글 목록으로 이동`, `이 탭 닫기` 두 버튼만 보인다.

### 5.2 비교안 A

- [ ] 댓글마다 작성자, 작성 시각, 도움이 돼요, 답글 보기, 댓글 정보 보기 등이 각각 탭 이동 지점으로 노출된다.
- [ ] 댓글 정보 대화상자를 닫으면 원래 댓글 작업으로 돌아가지 않고 댓글 목록 제목 근처로 돌아온다.
- [ ] 과업 1에서 `최신순 + 후기 + 민지 댓글 답글 보기`가 수행된다.
- [ ] 과업 2에서 `도움이 많은 순 + 운영자 댓글 정보 보기 후 도움이 돼요`가 수행된다.
- [ ] 과업 3에서 `질문 댓글만 보기 + 준호 댓글 답글 보기`가 수행된다.

### 5.3 비교안 B

- [ ] 맨 앞의 `댓글 목록으로 바로 이동` 링크로 첫 진입 부담을 줄일 수 있다.
- [ ] 댓글은 하나의 선택 항목으로 제공되고 방향키로 이동할 수 있다.
- [ ] `선택한 댓글 작업` 영역에서 답글 보기, 댓글 정보 보기, 도움이 돼요를 이어서 수행할 수 있다.
- [ ] 댓글 정보 대화상자를 닫으면 방금 사용한 작업 버튼으로 초점이 돌아온다.
- [ ] 댓글 범위를 적용하면 댓글 목록 제목으로 초점이 이동한다.

## 6. 상품 옵션 선택 점검표

### 6.1 공통

- [ ] 홈 화면의 `상품 옵션 선택` 카드에서 상품 서비스 화면으로 들어갈 수 있다.
- [ ] 과업 준비 화면에서 현재 선택 상태와 목표 옵션 조합이 보인다.
- [ ] 상품 옵션 선택은 비교안 A가 먼저 나오고 그다음 비교안 B가 나온다.
- [ ] 수행 탭 하단에는 `옵션 선택으로 이동`, `이 탭 닫기` 두 버튼만 보인다.

### 6.2 비교안 A

- [ ] 상단 보조 링크와 상품 안내 링크를 지나야 옵션 선택 묶음에 도달한다.
- [ ] 각 옵션 줄마다 `선택`, `옵션 설명 보기`가 따로 있어 순차 이동이 길다.
- [ ] 옵션 설명 대화상자를 닫으면 원래 옵션 줄 대신 `옵션 선택` 제목 근처로 돌아온다.
- [ ] 과업 1에서 `크림색 + 15형 + 충전기 주머니 포함 + 장바구니 담기`가 수행된다.
- [ ] 과업 2에서 `13형 설명 보기 후 먹색 기본형 담기`가 수행된다.
- [ ] 과업 3에서 `올리브 + 15형 + 문서 주머니 포함 + 수량 2개 담기`가 수행된다.

### 6.3 비교안 B

- [ ] 맨 앞의 `옵션 선택으로 바로 이동` 링크로 첫 진입 부담을 줄일 수 있다.
- [ ] 색상, 크기, 추가 구성은 한 번만 들어간 뒤 방향키로 이동할 수 있다.
- [ ] 각 옵션 묶음 아래에서 `선택한 ... 설명 보기` 버튼을 사용할 수 있다.
- [ ] 옵션 설명 대화상자를 닫으면 방금 사용한 설명 보기 버튼으로 초점이 돌아온다.
- [ ] `현재 선택과 장바구니` 영역에서 수량 변경과 장바구니 담기를 이어서 수행할 수 있다.

## 7. 검색 결과 목록 점검표

### 7.1 공통

- [ ] 홈 화면의 `검색 결과 목록` 카드에서 검색 서비스 화면으로 들어갈 수 있다.
- [ ] 과업 준비 화면에서 현재 검색 조건과 목표 자료가 보인다.
- [ ] 검색 결과 목록은 비교안 A가 먼저 나오고 그다음 비교안 B가 나온다.
- [ ] 수행 탭 하단에는 `검색 결과로 이동`, `이 탭 닫기` 두 버튼만 보인다.

### 7.2 비교안 A

- [ ] 상단 보조 링크와 자료 조건 선택을 지나야 검색 결과에 도달한다.
- [ ] 자료마다 제목 링크, 갱신 시각, 공유, 저장, 바로 열기, 미리보기가 각각 따로 있어 순차 이동이 길다.
- [ ] 미리보기 대화상자를 닫으면 원래 자료 작업 대신 검색 결과 제목 근처로 돌아온다.
- [ ] 과업 1에서 `최신 자료 순 + 안내문 + 예약 변경 안내 미리보기 열었다가 닫기`가 수행된다.
- [ ] 과업 2에서 `제목순 + 질문답변 + 비대면 상담 연결 방법 저장`이 수행된다.
- [ ] 과업 3에서 `신청 서식 + 상담 준비 체크 목록 미리보기 후 바로 열기`가 수행된다.

### 7.3 비교안 B

- [ ] 맨 앞의 `검색 결과로 바로 이동` 링크로 첫 진입 부담을 줄일 수 있다.
- [ ] 자료는 하나의 선택 항목으로 제공되고 방향키로 이동할 수 있다.
- [ ] `선택한 자료 작업` 영역에서 미리보기, 저장, 바로 열기를 이어서 수행할 수 있다.
- [ ] 미리보기 대화상자를 닫으면 방금 사용한 작업 버튼으로 초점이 돌아온다.
- [ ] 조건 적용 뒤 검색 결과 제목으로 초점이 이동한다.

## 8. 설정 화면 점검표

### 8.1 공통

- [ ] 홈 화면의 `설정 화면` 카드에서 설정 서비스 화면으로 들어갈 수 있다.
- [ ] 과업 준비 화면에서 현재 설정 상태와 목표 설정 상태가 보인다.
- [ ] 설정 화면은 비교안 A가 먼저 나오고 그다음 비교안 B가 나온다.
- [ ] 수행 탭 하단에는 `설정 항목으로 이동`, `이 탭 닫기` 두 버튼만 보인다.

### 8.2 비교안 A

- [ ] 상단 보조 링크와 앞선 설정 묶음을 지나야 원하는 설정 묶음에 도달한다.
- [ ] 각 설정마다 현재 상태, 최근 변경, 설명 보기, 값 선택 버튼이 따로 있어 순차 이동이 길다.
- [ ] 설명 대화상자를 닫으면 원래 설정 버튼 대신 설정 화면 제목 근처로 돌아온다.
- [ ] 과업 1에서 `문자 알림 끄기 + 상담 하루 전 알림 켜기 + 알림 설정 저장`이 수행된다.
- [ ] 과업 2에서 `로그인 확인 단계 설명 열고 닫기 + 새 기기 로그인 알림 켜기 + 보안 설정 저장`이 수행된다.
- [ ] 과업 3에서 `글자 크기 110% + 높은 대비 켜기 + 화면 설정 저장`이 수행된다.

### 8.3 비교안 B

- [ ] 맨 앞의 `설정 항목으로 바로 이동` 링크로 첫 진입 부담을 줄일 수 있다.
- [ ] 설정 묶음은 한 번만 들어간 뒤 방향키로 이동할 수 있다.
- [ ] 같은 설정 묶음 안에서 설명 보기, 값 변경, 저장을 이어서 수행할 수 있다.
- [ ] 설명 대화상자를 닫으면 방금 사용한 설명 보기 버튼으로 초점이 돌아온다.
- [ ] 보안 설정과 화면 설정으로 이동할 때 앞선 다른 묶음을 길게 지나지 않는다.


## 9. 검색 세부 조건 점검표

### 9.1 공통

- [ ] 홈 화면의 `검색 세부 조건` 카드에서 서비스 화면으로 들어갈 수 있다.
- [ ] 과업 준비 화면에서 현재 세부 조건과 목표 자료가 보인다.
- [ ] 검색 세부 조건은 비교안 A가 먼저 나오고 그다음 비교안 B가 나온다.
- [ ] 수행 탭 하단에는 `세부 조건으로 이동`, `이 탭 닫기` 두 버튼만 보인다.

### 9.2 비교안 A

- [ ] 상단 보조 링크와 세부 조건 설명 링크를 지나야 기간, 자료 종류, 담당 부서, 대상, 첨부 조건에 도달한다.
- [ ] 기간, 자료 종류, 담당 부서, 대상, 첨부 조건이 각각 여러 개의 개별 버튼으로 흩어져 있어 순차 이동이 길다.
- [ ] 미리보기 대화상자를 닫으면 원래 자료 작업 대신 검색 결과 제목 근처로 돌아온다.
- [ ] 과업 1에서 `최근 7일 + 안내문 + 상담 운영팀 + 예약 변경 안내 미리보기 열었다가 닫기`가 수행된다.
- [ ] 과업 2에서 `질문답변 + 원격 지원팀 + 첨부 있는 자료만 보기 + 비대면 상담 연결 방법 저장`이 수행된다.
- [ ] 과업 3에서 `신청 서식 + 비대면 상담 + 최근 30일 + 상담 준비 체크 목록 미리보기 후 바로 열기`가 수행된다.

### 9.3 비교안 B

- [ ] 맨 앞의 `세부 조건으로 바로 이동` 링크로 첫 진입 부담을 줄일 수 있다.
- [ ] 기간, 자료 종류, 담당 부서, 대상은 각각 한 번의 탭 진입으로 바꿀 수 있다.
- [ ] 첨부 있는 자료만 보기 조건은 다른 조건들과 가까이 있어 짧게 적용할 수 있다.
- [ ] 자료는 하나의 선택 항목으로 제공되고 방향키로 이동할 수 있다.
- [ ] `선택한 자료 작업` 영역에서 미리보기, 저장, 바로 열기를 이어서 수행할 수 있다.
- [ ] 미리보기 대화상자를 닫으면 방금 사용한 작업 버튼으로 초점이 돌아온다.

## 10. 신청·결제 흐름 점검표

### 10.1 공통

- [ ] 홈 화면의 `신청·결제 흐름` 카드에서 서비스 화면으로 들어갈 수 있다.
- [ ] 과업 준비 화면에서 현재 신청 상태와 목표 신청 상태가 보인다.
- [ ] 신청·결제 흐름은 비교안 A가 먼저 나오고 그다음 비교안 B가 나온다.
- [ ] 수행 탭 하단에는 `신청 단계로 이동`, `이 탭 닫기` 두 버튼만 보인다.

### 10.2 비교안 A

- [ ] 상단 보조 링크와 여러 신청 단계 안내를 지나야 원하는 신청 단계에 도달한다.
- [ ] 각 항목마다 현재 선택, 최근 확인, 설명 보기, 값 선택 버튼이 따로 있어 순차 이동이 길다.
- [ ] 설명 대화상자를 닫으면 원래 항목 대신 신청 단계 제목 근처로 돌아온다.
- [ ] 과업 1에서 `비대면 초진 상담 + 연락처 확인 안내 켜기 + 카드 결제 + 신청서 제출`이 수행된다.
- [ ] 과업 2에서 `보호자 대리 신청 + 준비 안내 설명 열고 닫기 + 준비 안내 문자 + 계좌 이체 + 신청서 제출`이 수행된다.
- [ ] 과업 3에서 `재방문 빠른 상담 + 영수증 문자 + 간편 결제 + 신청서 제출`이 수행된다.

### 10.3 비교안 B

- [ ] 맨 앞의 `신청 단계로 바로 이동` 링크로 첫 진입 부담을 줄일 수 있다.
- [ ] 신청 단계는 한 번만 들어간 뒤 방향키로 이동할 수 있다.
- [ ] 같은 신청 단계 안에서 설명 보기와 값 변경을 이어서 수행할 수 있다.
- [ ] 설명 대화상자를 닫으면 방금 사용한 설명 보기 버튼으로 초점이 돌아온다.
- [ ] 제출 확인 단계에서 `신청서 제출` 버튼으로 과업을 마무리할 수 있다.

## 11. 결과와 저장 확인

- [ ] 각 서비스의 최종 비교 화면에서 결과 파일(JSON) 내려받기 링크가 동작한다.
- [ ] 사전 계산 기준과 실제 기록이 함께 표시된다.
- [ ] `docs` 폴더의 단계 보고서에 이번 변경 사항이 반영되어 있다.
- [ ] 설문지 주소를 넣었을 경우 결과 전달 링크가 올바르게 만들어진다.

## 12. 배포 메모

- [ ] 이번 배포에서 공개하는 서비스 유형을 README에 반영했다.
- [ ] 새 서비스 유형을 추가했을 때 홈 화면 카드 상태와 점검표 항목도 함께 갱신했다.
- [ ] 브라우저 자동화로 검증하지 못한 항목은 이 문서의 체크 결과로 대체한다.
- [ ] 공통 모듈을 바꾼 배포라면 최소 `예약 캘린더`, `댓글 목록`, `신청·결제 흐름` 3개 서비스에서 진입→과업 준비→새 탭 수행→완료 대화상자→자동 닫기 흐름을 다시 확인한다.
- [ ] 공통 모듈을 바꾼 배포라면 수행 탭 결과가 메인 창 요약에 반영되는지와 결과 파일(JSON) 생성까지 다시 확인한다.
